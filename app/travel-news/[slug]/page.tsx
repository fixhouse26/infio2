import {notFound} from "next/navigation";import Link from "next/link";import {travelUpdates,updateBySlug} from "@/lib/travel-news";
export function generateStaticParams(){return travelUpdates.map(x=>({slug:x.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const n=updateBySlug(slug);if(!n)return{};return{title:n.title,description:n.summary,alternates:{canonical:`/travel-news/${n.slug}`}}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const n=updateBySlug(slug);if(!n)notFound();const schema={"@context":"https://schema.org","@type":"NewsArticle",headline:n.title,datePublished:n.published,dateModified:n.updated,author:{"@type":"Organization",name:"InfiO2 Travel"},publisher:{"@type":"Organization",name:"InfiO2 Travel",url:"https://www.infio2.com"}};return <main><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/><article className="articlePage"><div className="articleHead"><span className="eyebrow coral">{n.category} · {n.destination}</span><h1>{n.title}</h1><p className="articleDek">{n.summary}</p><small>Published {n.published} · Updated {n.updated}</small></div><div className="articleBody"><h2>What this means for travelers</h2><p>{n.travelerImpact}</p><h2>Source & verification</h2><p>{n.verified} <a href={n.sourceUrl} target="_blank" rel="noopener noreferrer">View {n.sourceName} ↗</a></p><div className="editorialNotice"><strong>Travel-information disclaimer:</strong> This summary is general information, not legal, immigration, safety, health or booking advice. Information may change after publication. The linked government/provider source controls where its information conflicts with this summary.</div><div className="articleCta"><h2>Planning a trip affected by this update?</h2><p>Build your preliminary trip, then ask InfiO2 to verify current options before booking.</p><Link className="btn primary" href={`/build-my-trip?destination=${encodeURIComponent(n.destination)}`}>Build My Trip →</Link></div></div></article></main>}