import Link from "next/link";import {travelUpdates} from "@/lib/travel-news";
export const metadata={title:"Travel Intelligence & Updates | InfiO2",description:"Traveler-focused updates with clear source attribution and links to authoritative information.",alternates:{canonical:"/travel-news"}};
export default function Page(){return <main><section className="pageHero newsHero"><span className="eyebrow">INFiO2 TRAVEL INTELLIGENCE</span><h1>Know what could affect your trip.</h1><p>Traveler-focused updates with publication dates, source attribution and links to authoritative information. We summarize; the original source remains the authority.</p></section><section className="section"><div className="newsList">{travelUpdates.map(n=><article className="newsCard" key={n.slug}><span className="eyebrow coral">{n.category} · {n.destination}</span><h2><Link href={`/travel-news/${n.slug}`}>{n.title}</Link></h2><p>{n.summary}</p><small>Updated {n.updated} · Source: {n.sourceName}</small><br/><Link className="textLink" href={`/travel-news/${n.slug}`}>What this means for travelers →</Link></article>)}</div><div className="editorialNotice"><strong>Important:</strong> Travel Intelligence is informational and may not reflect every change in real time. Always confirm visa, entry, safety, health, carrier and operational requirements directly with the relevant government authority or travel provider.</div></section></main>}