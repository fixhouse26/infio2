import {notFound} from "next/navigation";import Link from "next/link";import {creators,stories} from "@/lib/stories";
export function generateStaticParams(){return Object.keys(creators).map(slug=>({slug}))}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const c=creators[slug as keyof typeof creators];if(!c)notFound();const items=stories.filter(s=>s.authorSlug===slug);return <main><section className="pageHero"><span className="eyebrow">INFiO2 CREATOR</span><h1>{c.name}</h1><p>{c.role}</p></section><section className="section narrow"><p className="lead">{c.bio}</p><h2>Stories & guides</h2>{items.map(s=><p key={s.slug}><Link href={`/stories/${s.slug}`}>{s.title} →</Link></p>)}<div className="editorialNotice">Creator opinions are their own. Material relationships, sponsorships, complimentary travel or affiliate relationships must be disclosed on applicable content.</div></section></main>}